#include "regex.h"

/*
This file is used to make importing the /lib library easier.
It creates a clear entry point to the library. All layout of
/lib will be described here.

import order:
    lib
    regex
    log
    string
    imports
*/